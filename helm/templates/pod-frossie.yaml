apiVersion: v1
kind: Pod
metadata:
  name: worblehat-frossie
  namespace: worblehat
  labels:
    owner: frossie
spec:
  containers:
  - name: worblehat
    image: docker.io/lsstsqre/worblehat:ajt-dev
    imagePullPolicy: Always
    env:
    - name: WORBLEHAT_BASE_HREF
      value: "/files/frossie"
    - name: WORBLEHAT_DIR
      value: "/mnt"
    - name: WORBLEHAT_TIMEOUT
      value: "604800"
    ports:
    - containerPort: 8000
      name: http
      protocol: TCP
    volumeMounts:
    - mountPath: /mnt
      name: share
  restartPolicy: Never
  securityContext:
    runAsUser: 3000003
    runAsGroup: 3000003
    fsGroup: 3000003
    runAsNonRoot: true
    supplementalGroups:
    - 3000003
    - 2000004
    - 2000003
  volumes:
  - name: share
    nfs:
      path: /share1
      server: 10.87.86.26
      # 10.87.86.26 is dev
      # 10.22.240.130 is int
      # 10.13.105.122 is prod
  

    
    
