apiVersion: v1
kind: Service
metadata:
  name: worblehat-frossie
  namespace: worblehat
  labels:
    owner: frossie
spec:
  type: "ClusterIP"
  ports:
    - port: 8000
      targetPort: 8000
      protocol: "TCP"
  selector:
    owner: frossie
