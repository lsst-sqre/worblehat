apiVersion: v1
kind: Service
metadata:
  name: worblehat-adam
  namespace: worblehat
  labels:
    owner: adam
spec:
  type: "ClusterIP"
  ports:
    - port: 8000
      targetPort: 8000
      protocol: "TCP"
  selector:
    owner: adam
